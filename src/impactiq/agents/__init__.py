"""Agents - the single-agent loop and the multi-agent pipeline.

The orchestrator, the technical / knowledge / context specialists, the
adjudicator, and the critic are roles inside ``multi_agent.py``;
``single_agent.py`` is the single-loop fallback. Typed findings live in
``contracts.py`` and the Foundry / Microsoft Agent Framework wiring in
``runtime.py``.
"""
